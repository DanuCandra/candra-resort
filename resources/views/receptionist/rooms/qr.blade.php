@extends('layouts.main')
@section('title', 'QR Kamar '.$room->room_number)
@section('content')
    <x-dashboard.page-heading :title="'QR Kamar '.$room->room_number" description="QR permanen mengidentifikasi kamar; akses layanan tetap memerlukan active stay dan verifikasi nomor telepon." :back="route('receptionist.rooms.show', $room)" />
    <div class="row justify-content-center"><div class="col-xl-7"><div class="card"><div class="card-body text-center p-lg-5"><span class="badge bg-light-primary text-primary mb-3">{{ $room->roomType->name }}</span><h3 class="fw-semibold">Candra Resort · Kamar {{ $room->room_number }}</h3><p class="text-muted">Pindai untuk membuka portal layanan kamar.</p><div class="d-inline-block border rounded-4 p-3 bg-white my-3"><img src="{{ route('receptionist.rooms.qr.image', $room) }}" alt="QR Kamar {{ $room->room_number }}" width="360" height="360" class="img-fluid"></div><div class="text-start bg-light rounded p-3 mb-4"><small class="text-muted d-block mb-1">Alamat yang tersimpan di QR</small><code class="text-break">{{ $accessUrl }}</code></div><div class="d-flex flex-wrap justify-content-center gap-2"><a href="{{ route('receptionist.rooms.qr.image', [$room, 'download' => 1]) }}" class="btn btn-primary"><i class="ti ti-download me-1"></i>Unduh PNG</a><a href="{{ route('receptionist.rooms.qr.print', $room) }}" target="_blank" class="btn btn-outline-secondary"><i class="ti ti-printer me-1"></i>Cetak</a><form method="POST" action="{{ route('receptionist.rooms.qr.regenerate', $room) }}" data-confirm="QR lama langsung tidak berlaku. Cetak dan pasang QR yang baru setelah melanjutkan." data-confirm-title="Buat ulang QR?">@csrf<button class="btn btn-outline-danger"><i class="ti ti-refresh me-1"></i>Regenerasi</button></form></div></div></div></div></div>
@endsection
