@extends('layouts.main')
@section('title','Metode Pembayaran')
@section('content')
    <x-dashboard.page-heading title="Metode Pembayaran" description="Metode offline dikelola dinamis; kanal Midtrans dipisahkan dari pencatatan manual."><a href="{{ route('receptionist.payment-methods.create') }}" class="btn btn-primary"><i class="ti ti-plus me-1"></i>Tambah Metode</a></x-dashboard.page-heading>
    <div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-9"><input type="search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Cari nama atau kode metode..."></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Cari</button><a href="{{ route('receptionist.payment-methods.index') }}" class="btn btn-outline-secondary">Reset</a></div></form><div class="table-responsive"><table class="table align-middle text-nowrap"><thead><tr><th>Metode</th><th>Jenis</th><th>Kanal</th><th>Transaksi</th><th>Urutan</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>
        @forelse($paymentMethods as $method)<tr><td><div class="d-flex align-items-center gap-3"><span class="round-40 rounded bg-light-primary d-flex align-items-center justify-content-center"><i class="ti {{ $method->is_online ? 'ti-world' : 'ti-wallet' }} text-primary"></i></span><div><strong>{{ $method->name }}</strong><small class="d-block text-muted">{{ $method->code }}</small></div></div></td><td>{{ str($method->type)->replace('_',' ')->title() }}</td><td><span class="badge {{ $method->channel === 'midtrans' ? 'bg-light-primary text-primary' : 'bg-light-secondary text-secondary' }}">{{ ucfirst($method->channel) }}</span></td><td>{{ $method->payments_count }}</td><td>{{ $method->sort_order }}</td><td><span class="badge {{ $method->is_active ? 'bg-light-success text-success' : 'bg-light-danger text-danger' }}">{{ $method->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td><div class="d-flex justify-content-end gap-2"><a href="{{ route('receptionist.payment-methods.edit',$method) }}" class="btn btn-sm btn-outline-primary"><i class="ti ti-edit"></i></a><form method="POST" action="{{ route('receptionist.payment-methods.destroy',$method) }}" data-confirm="Metode yang pernah digunakan akan dinonaktifkan agar histori pembayaran tetap utuh." data-confirm-title="Hapus metode pembayaran?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="ti ti-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">Belum ada metode pembayaran.</td></tr>@endforelse
    </tbody></table></div>{{ $paymentMethods->links() }}</div></div>
@endsection
