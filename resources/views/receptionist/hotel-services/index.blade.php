@extends('layouts.main')
@section('title','Layanan Hotel')
@section('content')
<x-dashboard.page-heading title="Layanan Hotel" description="Kelola layanan berbayar yang dapat dipesan tamu dari QR kamar."><a href="{{ route('receptionist.hotel-services.create') }}" class="btn btn-primary"><i class="ti ti-plus me-1"></i>Tambah Layanan</a></x-dashboard.page-heading>
<div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-6"><input type="search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Nama atau kode layanan..."></div><div class="col-md-3"><select name="category" class="form-select"><option value="">Semua kategori</option>@foreach(['massage'=>'Massage','spa'=>'Spa','laundry'=>'Laundry','transport'=>'Transport','extra_bed'=>'Extra Bed','other'=>'Lainnya'] as $value=>$label)<option value="{{ $value }}" @selected(request('category')===$value)>{{ $label }}</option>@endforeach</select></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Filter</button><a href="{{ route('receptionist.hotel-services.index') }}" class="btn btn-outline-secondary">Reset</a></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Layanan</th><th>Kategori</th><th>Harga</th><th>Jadwal</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($services as $service)<tr><td><div class="d-flex align-items-center gap-3">@if($service->image_path)<img src="{{ Storage::url($service->image_path) }}" class="rounded" style="width:54px;height:54px;object-fit:cover" alt="{{ $service->name }}">@else<span class="round-40 rounded bg-light-primary d-flex align-items-center justify-content-center"><i class="ti ti-spa text-primary"></i></span>@endif<div><strong>{{ $service->name }}</strong><small class="d-block text-muted">{{ $service->code }} · {{ $service->orders_count }} pesanan</small></div></div></td><td>{{ str($service->category)->replace('_',' ')->title() }}</td><td>Rp{{ number_format((float)$service->price,0,',','.') }}<small class="d-block text-muted">{{ str($service->price_unit)->replace('_',' ') }}</small></td><td>{{ $service->requires_schedule?'Wajib':'Opsional' }}</td><td><span class="badge {{ $service->is_active&&$service->is_available?'bg-light-success text-success':'bg-light-danger text-danger' }}">{{ $service->is_active&&$service->is_available?'Tersedia':'Tidak tersedia' }}</span></td><td><div class="d-flex justify-content-end gap-2"><a href="{{ route('receptionist.hotel-services.edit',$service) }}" class="btn btn-sm btn-outline-primary"><i class="ti ti-edit"></i></a><form method="POST" action="{{ route('receptionist.hotel-services.destroy',$service) }}" data-confirm="Layanan yang memiliki histori akan dinonaktifkan." data-confirm-title="Hapus layanan?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="ti ti-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">Belum ada layanan hotel.</td></tr>@endforelse</tbody></table></div>{{ $services->links() }}</div></div>
@endsection
