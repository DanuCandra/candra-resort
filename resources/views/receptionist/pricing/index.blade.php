@extends('layouts.main')
@section('title', 'Aturan Harga')
@section('content')
    <x-dashboard.page-heading title="Aturan Harga" description="Atur harga per malam berdasarkan periode, hari, dan prioritas."><a href="{{ route('receptionist.pricing.create') }}" class="btn btn-primary"><i class="ti ti-plus me-1"></i>Tambah Harga</a></x-dashboard.page-heading>
    <div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-9"><select name="room_type_id" class="form-select"><option value="">Semua tipe kamar</option>@foreach($roomTypes as $roomType)<option value="{{ $roomType->id }}" @selected((string)request('room_type_id') === (string)$roomType->id)>{{ $roomType->name }}</option>@endforeach</select></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Filter</button><a href="{{ route('receptionist.pricing.index') }}" class="btn btn-outline-secondary">Reset</a></div></form><div class="table-responsive"><table class="table align-middle text-nowrap"><thead><tr><th>Aturan</th><th>Tipe Kamar</th><th>Periode</th><th>Hari</th><th>Harga</th><th>Prioritas</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>
        @forelse($rates as $rate)<tr><td class="fw-semibold">{{ $rate->name }}</td><td>{{ $rate->roomType->name }}</td><td>{{ $rate->start_date?->format('d/m/Y') ?? 'Setiap waktu' }} @if($rate->end_date)– {{ $rate->end_date->format('d/m/Y') }}@endif</td><td>@if($rate->days_of_week){{ collect($rate->days_of_week)->map(fn($day) => [1=>'Sen',2=>'Sel',3=>'Rab',4=>'Kam',5=>'Jum',6=>'Sab',7=>'Min'][$day])->join(', ') }}@else Semua @endif</td><td class="fw-semibold">Rp{{ number_format((float)$rate->price_per_night,0,',','.') }}</td><td>{{ $rate->priority }}</td><td><span class="badge {{ $rate->is_active ? 'bg-light-success text-success' : 'bg-light-danger text-danger' }}">{{ $rate->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td><div class="d-flex justify-content-end gap-2"><a href="{{ route('receptionist.pricing.edit', $rate) }}" class="btn btn-sm btn-outline-primary"><i class="ti ti-edit"></i></a><form method="POST" action="{{ route('receptionist.pricing.destroy', $rate) }}" data-confirm="Aturan dengan histori reservasi akan dinonaktifkan." data-confirm-title="Hapus aturan harga?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="ti ti-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="8" class="text-center text-muted py-5">Belum ada aturan harga.</td></tr>@endforelse
    </tbody></table></div>{{ $rates->links() }}</div></div>
@endsection
