@extends('layouts.main')
@section('title','Permintaan Tamu')
@section('content')<x-dashboard.page-heading title="Permintaan Tamu" description="Antrean housekeeping, amenity, dan bantuan; prioritas mendesak ditampilkan lebih dulu."/><div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-4"><select name="type" class="form-select"><option value="">Semua jenis</option>@foreach(['housekeeping'=>'Housekeeping','assistance'=>'Bantuan','amenity'=>'Amenity','other'=>'Lainnya'] as $value=>$label)<option value="{{ $value }}" @selected(request('type')===$value)>{{ $label }}</option>@endforeach</select></div><div class="col-md-4"><select name="status" class="form-select"><option value="">Semua status</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select></div><div class="col-md-4 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Filter</button><a href="{{ route('receptionist.guest-requests.index') }}" class="btn btn-outline-secondary">Reset</a></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Permintaan</th><th>Tamu/Kamar</th><th>Jenis</th><th>Prioritas</th><th>Waktu</th><th>Status</th><th></th></tr></thead><tbody>@forelse($requests as $item)<tr><td><strong>{{ $item->title }}</strong><small class="d-block text-muted">{{ $item->request_code }}</small></td><td>{{ $item->stay->guest_name }}<small class="d-block text-muted">Kamar {{ $item->room->room_number }}</small></td><td>{{ str($item->type)->title() }}</td><td><span class="badge {{ in_array($item->priority,['urgent','high'])?'bg-light-danger text-danger':'bg-light-secondary text-secondary' }}">{{ str($item->priority)->title() }}</span></td><td>{{ $item->requested_at->format('d M H:i') }}</td><td><span class="badge bg-light-{{ $item->status->badgeClass() }} text-{{ $item->status->badgeClass() }}">{{ $item->status->label() }}</span></td><td class="text-end"><a href="{{ route('receptionist.guest-requests.show',$item) }}" class="btn btn-sm btn-outline-primary">Proses</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">Belum ada permintaan tamu.</td></tr>@endforelse</tbody></table></div>{{ $requests->links() }}</div></div>@endsection
