@extends('layouts.main')
@section('title','Folio Tamu')
@section('content')<x-dashboard.page-heading title="Folio Tamu" description="Tagihan berjalan dari kamar, F&B, layanan hotel, dan seluruh pembayaran tamu."/><div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-7"><input type="search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Nomor folio, booking, atau nama tamu..."></div><div class="col-md-2"><select name="status" class="form-select"><option value="">Semua status</option><option value="open" @selected(request('status')==='open')>Aktif</option><option value="closed" @selected(request('status')==='closed')>Ditutup</option></select></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Filter</button><a href="{{ route('receptionist.folios.index') }}" class="btn btn-outline-secondary">Reset</a></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Folio</th><th>Tamu/Kamar</th><th>Reservasi</th><th>Item</th><th>Total</th><th>Dibayar</th><th>Saldo</th><th>Status</th><th></th></tr></thead><tbody>@forelse($folios as $folio)<tr><td><strong>{{ $folio->folio_number }}</strong></td><td>{{ $folio->stay->guest_name }}<small class="d-block text-muted">Kamar {{ $folio->stay->room->room_number }}</small></td><td>{{ $folio->reservation->booking_code }}<small class="d-block text-muted">{{ $folio->reservation->roomType->name }}</small></td><td>{{ $folio->items_count }}</td><td>Rp{{ number_format((float)$folio->total_amount,0,',','.') }}</td><td class="text-success">Rp{{ number_format((float)$folio->paid_amount,0,',','.') }}</td><td class="fw-semibold {{ (float)$folio->balance_amount>0?'text-danger':'text-success' }}">Rp{{ number_format((float)$folio->balance_amount,0,',','.') }}</td><td><span class="badge {{ $folio->status==='open'?'bg-light-warning text-warning':'bg-light-success text-success' }}">{{ $folio->status==='open'?'Aktif':'Ditutup' }}</span></td><td><a href="{{ route('receptionist.folios.show',$folio) }}" class="btn btn-sm btn-outline-primary">Detail</a></td></tr>@empty<tr><td colspan="9" class="text-center text-muted py-5">Belum ada folio tamu.</td></tr>@endforelse</tbody></table></div>{{ $folios->links() }}</div></div>@endsection
