@extends('layouts.main')
@section('title','Pesanan Layanan Hotel')
@section('content')<x-dashboard.page-heading title="Pesanan Layanan Hotel" description="Kelola layanan spa, massage, laundry, transport, dan layanan berbayar lainnya."/><div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-6"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Kode pesanan atau nama tamu..."></div><div class="col-md-3"><select name="status" class="form-select"><option value="">Semua status</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Filter</button><a href="{{ route('receptionist.service-orders.index') }}" class="btn btn-outline-secondary">Reset</a></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Pesanan</th><th>Tamu/Kamar</th><th>Layanan</th><th>Jadwal</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr><td><strong>{{ $order->order_code }}</strong><small class="d-block text-muted">{{ $order->created_at->format('d M H:i') }}</small></td><td>{{ $order->stay->guest_name }}<small class="d-block text-muted">Kamar {{ $order->room->room_number }}</small></td><td>{{ $order->service->name }}<small class="d-block text-muted">{{ (float)$order->quantity }} × Rp{{ number_format((float)$order->unit_price,0,',','.') }}</small></td><td>{{ $order->scheduled_at?->format('d M H:i') ?? '-' }}</td><td>Rp{{ number_format((float)$order->total_amount,0,',','.') }}</td><td><span class="badge bg-light-{{ $order->status->badgeClass() }} text-{{ $order->status->badgeClass() }}">{{ $order->status->label() }}</span></td><td class="text-end"><a href="{{ route('receptionist.service-orders.show',$order) }}" class="btn btn-sm btn-outline-primary">Proses</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">Belum ada pesanan layanan.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}</div></div>@endsection
