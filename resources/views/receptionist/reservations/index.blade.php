@extends('layouts.main')
@section('title','Reservasi')
@section('content')
    <x-dashboard.page-heading title="Reservasi" description="Pantau reservasi online dan walk-in dari satu tempat."><a href="{{ route('receptionist.reservations.walk-in.create') }}" class="btn btn-primary"><i class="ti ti-walk me-1"></i>Buat Walk-In</a></x-dashboard.page-heading>
    <div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-lg-5"><input type="search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Kode, nama, atau telepon tamu..."></div><div class="col-lg-3"><select name="status" class="form-select"><option value="">Semua status</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ str($status->value)->replace('_',' ')->title() }}</option>@endforeach</select></div><div class="col-lg-2"><input type="date" name="arrival_date" value="{{ request('arrival_date') }}" class="form-control"></div><div class="col-lg-2 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Filter</button><a class="btn btn-outline-secondary" href="{{ route('receptionist.reservations.index') }}"><i class="ti ti-refresh"></i></a></div></form>
        <div class="table-responsive"><table class="table align-middle text-nowrap"><thead><tr><th>Reservasi</th><th>Tamu</th><th>Menginap</th><th>Kamar</th><th>Pembayaran</th><th>Status</th><th></th></tr></thead><tbody>@forelse($reservations as $reservation)<tr><td><a class="fw-semibold text-dark" href="{{ route('receptionist.reservations.show',$reservation) }}">{{ $reservation->booking_code }}</a><small class="d-block text-muted">{{ $reservation->source==='walk_in'?'Walk-In':'Online' }}</small></td><td>{{ $reservation->guest_name }}<small class="d-block text-muted">{{ $reservation->guest_phone }}</small></td><td>{{ $reservation->check_in_date->format('d M') }} – {{ $reservation->check_out_date->format('d M Y') }}<small class="d-block text-muted">{{ $reservation->total_nights }} malam</small></td><td>{{ $reservation->roomType->name }}<small class="d-block text-muted">{{ $reservation->room?'Kamar '.$reservation->room->room_number:'Belum ditentukan' }}</small></td><td>Rp{{ number_format((float)$reservation->grand_total,0,',','.') }}<small class="d-block text-muted">{{ str($reservation->payment_status->value)->title() }}</small></td><td><span class="badge bg-light-primary text-primary">{{ str($reservation->status->value)->replace('_',' ')->title() }}</span></td><td><a href="{{ route('receptionist.reservations.show',$reservation) }}" class="btn btn-sm btn-outline-secondary"><i class="ti ti-eye"></i></a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">Tidak ada reservasi.</td></tr>@endforelse</tbody></table></div>{{ $reservations->links() }}
    </div></div>
@endsection
