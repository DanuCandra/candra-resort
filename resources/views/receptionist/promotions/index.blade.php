@extends('layouts.main')
@section('title','Promosi')
@section('content')
    <x-dashboard.page-heading title="Promosi" description="Kelola kode diskon, periode, kuota, dan tipe kamar yang memenuhi syarat."><a href="{{ route('receptionist.promotions.create') }}" class="btn btn-primary"><i class="ti ti-plus me-1"></i>Tambah Promosi</a></x-dashboard.page-heading>
    <div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-9"><input type="search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Cari nama atau kode promosi..."></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Cari</button><a href="{{ route('receptionist.promotions.index') }}" class="btn btn-outline-secondary">Reset</a></div></form><div class="table-responsive"><table class="table align-middle text-nowrap"><thead><tr><th>Promosi</th><th>Diskon</th><th>Periode</th><th>Pemakaian</th><th>Tipe Kamar</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>
        @forelse($promotions as $promotion)<tr><td><strong>{{ $promotion->name }}</strong><small class="d-block text-primary">{{ $promotion->code }}</small></td><td>{{ $promotion->discount_type === 'percent' ? number_format((float)$promotion->discount_value,0). '%' : 'Rp'.number_format((float)$promotion->discount_value,0,',','.') }}</td><td>{{ $promotion->starts_at?->format('d/m/Y') ?? 'Tanpa batas' }} @if($promotion->ends_at)– {{ $promotion->ends_at->format('d/m/Y') }}@endif</td><td>{{ $promotion->used_count }} / {{ $promotion->usage_quota ?? '∞' }}</td><td>{{ $promotion->roomTypes->isEmpty() ? 'Semua' : $promotion->roomTypes->pluck('name')->join(', ') }}</td><td><span class="badge {{ $promotion->is_active ? 'bg-light-success text-success' : 'bg-light-danger text-danger' }}">{{ $promotion->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td><div class="d-flex justify-content-end gap-2"><a href="{{ route('receptionist.promotions.edit',$promotion) }}" class="btn btn-sm btn-outline-primary"><i class="ti ti-edit"></i></a><form method="POST" action="{{ route('receptionist.promotions.destroy',$promotion) }}" data-confirm="Promosi dengan riwayat reservasi akan dinonaktifkan." data-confirm-title="Hapus promosi?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="ti ti-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">Belum ada promosi.</td></tr>@endforelse
    </tbody></table></div>{{ $promotions->links() }}</div></div>
@endsection
