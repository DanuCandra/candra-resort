@extends('layouts.main')
@section('title', 'Daftar Menu F&B')
@section('content')
    <x-dashboard.page-heading title="Daftar Menu F&B" description="Atur harga, foto, ketersediaan, dan estimasi persiapan menu."><a href="{{ route('receptionist.menu-items.create') }}" class="btn btn-primary"><i class="ti ti-plus me-1"></i>Tambah Menu</a></x-dashboard.page-heading>
    <div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-6"><input type="search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Cari menu..."></div><div class="col-md-3"><select name="category" class="form-select"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string)request('category') === (string)$category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Filter</button><a href="{{ route('receptionist.menu-items.index') }}" class="btn btn-outline-secondary">Reset</a></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Menu</th><th>Kategori</th><th>Harga</th><th>Persiapan</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>
        @forelse($menuItems as $item)<tr><td><div class="d-flex align-items-center gap-3">@if($item->image_path)<img src="{{ Storage::url($item->image_path) }}" alt="{{ $item->name }}" class="rounded" style="width:54px;height:54px;object-fit:cover">@else<span class="round-40 rounded bg-light-primary d-flex align-items-center justify-content-center"><i class="ti ti-soup text-primary"></i></span>@endif<div><strong>{{ $item->name }}</strong><small class="d-block text-muted">{{ $item->order_items_count }} kali dipesan</small></div></div></td><td>{{ $item->category?->name ?? 'Tanpa kategori' }}</td><td>Rp{{ number_format((float)$item->price, 0, ',', '.') }}</td><td>{{ $item->preparation_minutes ? $item->preparation_minutes.' menit' : '-' }}</td><td><span class="badge {{ $item->is_active && $item->is_available ? 'bg-light-success text-success' : 'bg-light-danger text-danger' }}">{{ $item->is_active && $item->is_available ? 'Tersedia' : 'Tidak tersedia' }}</span></td><td><div class="d-flex justify-content-end gap-2"><a href="{{ route('receptionist.menu-items.edit', $item) }}" class="btn btn-sm btn-outline-primary"><i class="ti ti-edit"></i></a><form method="POST" action="{{ route('receptionist.menu-items.destroy', $item) }}" data-confirm="Menu yang memiliki histori pesanan akan dinonaktifkan, bukan dihapus." data-confirm-title="Hapus menu?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="ti ti-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">Belum ada menu.</td></tr>@endforelse
        </tbody></table></div>{{ $menuItems->links() }}</div></div>
@endsection
