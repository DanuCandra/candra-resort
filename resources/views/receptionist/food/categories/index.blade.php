@extends('layouts.main')
@section('title', 'Kategori F&B')
@section('content')
    <x-dashboard.page-heading title="Kategori F&B" description="Kelompokkan makanan dan minuman yang tampil pada portal kamar."><a href="{{ route('receptionist.food-categories.create') }}" class="btn btn-primary"><i class="ti ti-plus me-1"></i>Tambah Kategori</a></x-dashboard.page-heading>
    <div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-9"><input type="search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Cari kategori..."></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Cari</button><a href="{{ route('receptionist.food-categories.index') }}" class="btn btn-outline-secondary">Reset</a></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Kategori</th><th>Jumlah Menu</th><th>Urutan</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>
        @forelse($categories as $category)<tr><td><strong>{{ $category->name }}</strong><small class="d-block text-muted">{{ $category->description ?: 'Tanpa deskripsi' }}</small></td><td>{{ $category->menu_items_count }}</td><td>{{ $category->sort_order }}</td><td><span class="badge {{ $category->is_active ? 'bg-light-success text-success' : 'bg-light-danger text-danger' }}">{{ $category->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td><div class="d-flex justify-content-end gap-2"><a href="{{ route('receptionist.food-categories.edit', $category) }}" class="btn btn-sm btn-outline-primary"><i class="ti ti-edit"></i></a><form method="POST" action="{{ route('receptionist.food-categories.destroy', $category) }}" data-confirm="Kategori yang masih memiliki menu akan dinonaktifkan untuk menjaga relasi data." data-confirm-title="Hapus kategori?"><input type="hidden" name="_token" value="{{ csrf_token() }}">@method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="ti ti-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">Belum ada kategori F&B.</td></tr>@endforelse
        </tbody></table></div>{{ $categories->links() }}</div></div>
@endsection
