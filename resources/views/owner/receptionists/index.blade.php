@extends('layouts.main')
@section('title','Receptionist')
@section('content')
    <x-dashboard.page-heading title="Receptionist" description="Kelola akun staf front office dan status akses sistem."><a href="{{ route('owner.receptionists.create') }}" class="btn btn-primary"><i class="ti ti-user-plus me-1"></i>Tambah Receptionist</a></x-dashboard.page-heading>
    <div class="card"><div class="card-body"><form method="GET" class="row g-2 mb-4"><div class="col-md-7"><input type="search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Cari nama, email, atau kode pegawai..."></div><div class="col-md-3"><select name="active" class="form-select"><option value="">Semua status</option><option value="1" @selected(request('active')==='1')>Aktif</option><option value="0" @selected(request('active')==='0')>Nonaktif</option></select></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-outline-primary flex-grow-1">Filter</button><a href="{{ route('owner.receptionists.index') }}" class="btn btn-outline-secondary"><i class="ti ti-refresh"></i></a></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Receptionist</th><th>Kode</th><th>Kontak</th><th>Aktivitas</th><th>Login Terakhir</th><th>Status</th><th></th></tr></thead><tbody>@forelse($receptionists as $receptionist)<tr><td><a class="fw-semibold text-dark" href="{{ route('owner.receptionists.show',$receptionist) }}">{{ $receptionist->name }}</a><small class="d-block text-muted">{{ '@'.$receptionist->username }}</small></td><td>{{ $receptionist->employee_code }}</td><td>{{ $receptionist->email }}<small class="d-block text-muted">{{ $receptionist->phone }}</small></td><td>{{ $receptionist->received_payments_count }} pembayaran<small class="d-block text-muted">{{ $receptionist->audit_logs_count }} audit</small></td><td>{{ $receptionist->last_login_at?->translatedFormat('d M Y H:i')?:'Belum pernah' }}</td><td><span class="badge {{ $receptionist->is_active?'bg-light-success text-success':'bg-light-danger text-danger' }}">{{ $receptionist->is_active?'Aktif':'Nonaktif' }}</span></td><td><div class="d-flex gap-2"><a href="{{ route('owner.receptionists.edit',$receptionist) }}" class="btn btn-sm btn-outline-primary"><i class="ti ti-edit"></i></a><form method="POST" action="{{ route('owner.receptionists.toggle',$receptionist) }}" data-confirm="Akses login Receptionist akan {{ $receptionist->is_active?'dinonaktifkan':'diaktifkan' }}." data-confirm-title="Ubah status akun?">@csrf<button class="btn btn-sm btn-outline-{{ $receptionist->is_active?'danger':'success' }}"><i class="ti ti-power"></i></button></form></div></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">Belum ada Receptionist.</td></tr>@endforelse</tbody></table></div>{{ $receptionists->links() }}</div></div>
@endsection
