@extends('layouts.guest')
@section('title','Pesanan Layanan Saya')
@section('content')<section class="auth-section"><div class="container"><div class="d-flex flex-wrap justify-content-between align-items-center mb-5"><div><p class="text-uppercase mb-1" style="letter-spacing:2px;color:#dfa974;font-weight:700">Kamar {{ $access->room->room_number }}</p><h2>Pesanan Layanan</h2></div><div><a href="{{ route('room-service.services.index') }}" class="sona-button mr-2">Pesan Layanan</a><a href="{{ route('room-service.home') }}" class="btn btn-outline-secondary">Portal</a></div></div><div class="auth-card"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Kode</th><th>Layanan</th><th>Jadwal</th><th>Status</th><th class="text-right">Total</th></tr></thead><tbody>@forelse($orders as $order)<tr><td><a href="{{ route('room-service.services.show',$order) }}"><strong>{{ $order->order_code }}</strong></a></td><td>{{ $order->service->name }}</td><td>{{ $order->scheduled_at?->translatedFormat('d M Y H:i') ?? '-' }}</td><td><span class="badge badge-{{ $order->status->badgeClass() }}">{{ $order->status->label() }}</span></td><td class="text-right">Rp{{ number_format((float)$order->total_amount,0,',','.') }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">Belum ada pesanan layanan.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}</div></div></section>@endsection
