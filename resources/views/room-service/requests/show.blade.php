@extends('layouts.guest')
@section('title',$guestRequest->request_code)
@section('content')<section class="auth-section"><div class="container"><div class="d-flex justify-content-between align-items-center mb-5"><div><p class="text-uppercase mb-1" style="letter-spacing:2px;color:#dfa974;font-weight:700">Detail Permintaan</p><h2>{{ $guestRequest->request_code }}</h2></div><a href="{{ route('room-service.requests.index') }}" class="btn btn-outline-secondary">Kembali</a></div><div class="row justify-content-center"><div class="col-lg-8"><div class="auth-card"><div class="d-flex flex-wrap justify-content-between mb-4"><div><span class="text-muted">{{ str($guestRequest->type)->title() }}</span><h3>{{ $guestRequest->title }}</h3></div><span class="badge badge-{{ $guestRequest->status->badgeClass() }} align-self-start p-2">{{ $guestRequest->status->label() }}</span></div><p>{{ $guestRequest->description ?: 'Tidak ada detail tambahan.' }}</p><hr><div class="row"><div class="col-md-4"><small class="text-muted">Dikirim</small><strong class="d-block">{{ $guestRequest->requested_at->translatedFormat('d M Y H:i') }}</strong></div><div class="col-md-4"><small class="text-muted">Prioritas</small><strong class="d-block">{{ str($guestRequest->priority)->title() }}</strong></div><div class="col-md-4"><small class="text-muted">Ditangani oleh</small><strong class="d-block">{{ $guestRequest->handledBy?->name ?? 'Menunggu Receptionist' }}</strong></div></div></div></div></div></div></section>@endsection
